{
  "name": "@unify-js/vitepress-mermaid-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @unify-js/vitepress-mermaid build",
    "dev": "pnpm --filter @unify-js/vitepress-mermaid dev",
    "docs:dev": "pnpm --filter @unify-js/vitepress-mermaid-docs dev",
    "docs:build": "pnpm --filter @unify-js/vitepress-mermaid-docs build",
    "docs:preview": "pnpm --filter @unify-js/vitepress-mermaid-docs preview",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "vitepress",
    "mermaid",
    "diagram",
    "preview",
    "plugin"
  ],
  "author": "Luo Huidong",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.5.0"
  }
}
